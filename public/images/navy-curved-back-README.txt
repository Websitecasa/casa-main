Place the attached navy-blue chair image here and name it exactly:

  navy-curved-back.jpg

Put this file in the `public/images/` folder so the app can serve it at `/images/navy-curved-back.jpg`.

If you prefer a different filename, update the `image` value for `nc5` in `constants.tsx` accordingly.
