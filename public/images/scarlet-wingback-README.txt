Place the attached red wingback (Scarlet Wingback) image here and name it exactly:

  scarlet-wingback.jpg

Put this file in the `public/images/` folder so the app can serve it at `/images/scarlet-wingback.jpg`.

If you prefer a different filename, update the `image` value for `nc7` in `constants.tsx` accordingly.
